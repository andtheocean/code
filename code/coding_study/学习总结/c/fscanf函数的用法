fscanf(fp,format,..地址）
format:%[*][width][modifications]type   
×用于匹配一定类型的任意数据。
modifications用来修饰数据类型 如h加上d前面表示短整型

格式匹配的时候  如果只有前面的一部分匹配成功，如果后面的部分是空格 或换行  而非有效数据的话  可以继续向后面读取。
但是如果有有效的数据是由于格式不符合而匹配不成功的话将不能断续读取。


fgets(s,size,fp)
用来读取一行数据  遇换行结束  并将换行符也写入  完成之后在末尾加 /0
